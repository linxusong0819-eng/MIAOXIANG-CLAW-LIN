# 妙想流式接口演示

## 本地运行

```powershell
py -3 -m pip install -r requirements.txt
py -3 -m streamlit run app.py
```

## 密钥

把 `EM_API_KEY` 放到 Streamlit Cloud 的 Secrets，或者本地的 `~/.streamlit/secrets.toml`。

## 部署

Streamlit Cloud 的入口文件是 `app.py`。
